# Mekai API — Lightweight PaddleOCR backend
#
# Target: Railway free tier (500 MB RAM limit)
# Stack:  FastAPI + PaddleOCR (CPU-only) + OPUS-MT (Helsinki-NLP/opus-mt-ja-en)
#
# Install (x86_64):
#   pip install -r requirements.txt
#   python main.py --install-translate   # one-time OPUS-MT model download (~300 MB)
#
# ARM64 / Apple Silicon: see README.md for workarounds.

# ── Web framework ──────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9        # required for UploadFile support

# ── Image handling ─────────────────────────────────────────────
Pillow>=10.0,<11.0

# ── OCR engine (CPU-only PaddlePaddle + PaddleOCR) ────────────
# paddlepaddle CPU-only — ~90 MB, no CUDA/cuDNN deps
paddlepaddle>=2.6.0,<3.0
paddleocr>=2.9.0,<3.0

# ── Translation — OPUS-MT ja→en (Helsinki-NLP/opus-mt-ja-en) ──
# torch is required for OPUS-MT models (2.6+ required for security fix)
torch>=2.6.0,<2.7.0
transformers>=4.40,<5.0
sentencepiece>=0.2.0
